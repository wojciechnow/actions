on: [push]

jobs:
  get_payload_data:
    runs-on: ubuntu-latest
    name: Get payload data
    steps:
    - name: Get payload data
      uses: wojciechnow/actions@moreinfo
    # Use the output from the `hello` step
    # name: Get the output time
    # run: echo "The time was ${{ steps.hello.outputs.time }}"
